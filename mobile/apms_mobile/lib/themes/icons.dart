import 'package:apms_mobile/themes/colors.dart';
import 'package:flutter/material.dart';

const locationOnIcon = Icon(Icons.location_on_sharp, size: 14, color: deepBlue);
const locationOffIcon =
    Icon(Icons.location_off_sharp, size: 14, color: deepBlue);
const qrIcon = Icon(Icons.qr_code_2_rounded, size: 30, color: deepBlue);
const searchIcon = Icon(
  Icons.search_outlined,
  size: 20,
  color: deepBlue,
);
const distanceIcon = Padding(
    padding: EdgeInsets.only(right: 10),
    child: Icon(
      Icons.call_made_rounded,
      size: 14,
    ));
const profileIcon = Icon(Icons.account_circle_rounded);
const transactionIcon = Icon(Icons.account_balance_rounded);
const aboutIcon = Icon(Icons.question_mark_rounded);
const navigateIcon = Icon(Icons.arrow_forward_ios_rounded);
const addIcon = Icon(
  Icons.add,
  color: deepBlue,
  size: 35,
);
const paypalIcon = Icon(Icons.paypal_rounded);
const transactionSuccessfullyIcon = Icon(
  Icons.check_circle_outline_rounded,
  size: 80,
  color: green,
);
const feedbackIcon = Icon(Icons.comment_rounded);
